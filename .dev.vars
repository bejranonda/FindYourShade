# Development variables (NOT committed to git)
# Copy this file to .dev.vars.local and add your D1 database ID

# D1 Database binding name (must match wrangler.toml)
# Run: wrangler d1 create DB --name=findyourshade-db
# Then copy the database ID here
# D1_DATABASE_ID=your-database-id-here
